build.gradle.kts 固定 `buildToolsVersion = "35.0.0"` 以避免从 dl.google.com 自动下载；release 构建启用 minify/shrink 并引用 `proguard-rules.pro`，但注释提示发布前需手动配置正式签名密钥。前台服务使用 `FOREGROUND_SERVICE_TYPE_SPECIAL_USE`，需在 Manifest 中声明对应权限。